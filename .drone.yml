kind: pipeline
type: docker
name: staging-automated

trigger:
  branch:
    - staging
  event:
    - push

steps:
  - name: simulate-staging-deploy
    image: alpine
    commands:
      - echo "🚀 Déploiement AUTOMATIQUE sur staging"
      - echo "Simulation construction de l'application..."
      - echo "Simulation déploiement sur serveur staging..."
      - echo "✅ Staging déployé avec succès"

---
kind: pipeline
type: docker
name: production-manual

trigger:
  branch:
    - main
  event:
    - push

steps:
  - name: build-production
    image: alpine
    commands:
      - echo "🏗 Construction pour production"
      - echo "Simulation npm install"
      - echo "Simulation npm run build"

  - name: deploy-production
    image: alpine
    commands:
      - echo "🛑 ATTENTIO Ceci déploierait en PRODUCTION"
      - echo "Simulation déploiement sur serveurs production..."
      - echo "✅ Production déployée"
    when:
      status:
        - success
      action:
        - promote
